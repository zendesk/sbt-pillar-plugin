language: scala

scala:
  - 2.10.5

jdk:
  - oraclejdk8

sudo: false

services:
  - cassandra

cache:
  directories:
    - $HOME/.ivy2/cache
    - $HOME/.sbt/boot/

script:
  - sbt ++$TRAVIS_SCALA_VERSION clean coverage compile test coverageReport codacyCoverage
